# Secrets only — non-sensitive settings live in config.yaml

# Required when config.yaml notification is discord
BOT_TOKEN=

# Required when config.yaml notification is bark
BARK_KEY=

# Runtime environment: dev | prod
ENV=dev
