services:
  alert-bot:
    build: .
    container_name: yahoo-mercari-alert-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      ENV: prod
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml:ro
    depends_on: []
