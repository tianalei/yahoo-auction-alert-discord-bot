# Core Discord bot functionality
hikari-lightbulb>=2.3.1 # Includes hikari
# hikari-miru # 如果您用了 miru 的 UI 组件，请取消注释

# HTTP requests and web scraping
requests>=2.30.0
beautifulsoup4>=4.13.4 # Includes soupsieve
httpx>=0.23.3 # For easygoogletranslate and mercapi

# Data handling
dataset>=1.6.0 # Includes SQLAlchemy, normality, banal
python-dotenv>=0.21.1
PyYAML>=6.0

# Specific APIs
easygoogletranslate>=0.0.4
mercapi>=0.3.0

# Performance (optional but recommended by hikari)
orjson>=3.8.12

# Common low-level dependencies (pip should resolve these from the above)
# certifi
# idna
# urllib3
# charset-normalizer
# attrs
# typing_extensions
# anyio (and its dependencies like exceptiongroup, sniffio)
# aiohttp (and its dependencies like aiosignal, async-timeout, frozenlist, multidict, yarl)
# cryptography (and its dependencies like cffi, pycparser)
# httpcore (and its dependencies like h11)
# rfc3986